// Crea una función que muestre los números del 1 al 100

const showNumbers = () => {
  // 👇🏻 Añade tu lógica aquí
  for(let x = 1; x <= 100; x++){
    console.log(x);
  }
  // 👆🏻
};

showNumbers();
